export const citiesSelector = (state) => state.cities.entities;

export const citiesLoadedSelector = (state) => state.cities.loaded;
export const citiesLoadingSelector = (state) => state.cities.loading;
